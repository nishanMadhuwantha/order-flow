
const OrderListPage = () => {
  const handleRowClick = () => {};

  return (
    <div>
      <button onClick={() => handleRowClick()}>
        Go to Order  1
      </button>
    </div>
  );
};

export default OrderListPage;